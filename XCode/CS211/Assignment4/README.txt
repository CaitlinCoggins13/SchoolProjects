To Compile:

Navigate to the folder Assignment and go inside it.

Run the line “javac -d bin src/*.java” to compile.

To Run:

Compile first.

Run the line “java -cp bin HashTableText” to run the program.

NOTE:  I have no idea why, but the first ‘abacus’ in the text file hashes into the wrong bucket.  Nothing else is in the wrong bucket, just the first abacus.  I looked through everything to try to figure out why this is happening, but just left it in the end because every other word works.